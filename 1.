class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        for(int i=0;i < sizeof(nums);i++){
            if((nums[i]+nums[i+1])==target)
            cout<<i<<i+1;
        }
    }
};
